using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Intro : LevelController
{
    // Start is called before the first frame update
    void Start()
    {
        LoadNextScene(3);
    }
}
